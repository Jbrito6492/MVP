import React from "react";

const Home = (props) => {

//   const changeView = (state) => {
// if(auth)
//   };

  return (
    <div>
      <h3>Welcome</h3>
    </div>
  );
};

export default {
  component: Home,
};
